{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p .vercel-output && echo '{\"version\": 3}' > .vercel-output/config.json",
    "build-react": "cd Experiment-12_ReactJS_Weather-app/weather-app/weather-app && npm install && npm run build && cd ../../../../Experiment-13_ReactJS_Todo-app/todo-app && npm install && npm run build"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}